#include <Led blink 18f2431.h>

void main()
{

   //Example blinking LED program
   while(true)
   {
      //output_low(PIN_A2);
      //delay_ms(500);
      output_high(PIN_A2);
      //delay_ms(500);
   }

}
