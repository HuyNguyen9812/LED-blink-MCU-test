#include <Led blink 18f2431.h>

void main()
{

   //Example blinking LED program
   while(true)
   {
      output_low(PIN_A5);
      delay_ms(500);
      output_high(PIN_A5);
      delay_ms(500);
   }

}
